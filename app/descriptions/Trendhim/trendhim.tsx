import React from "react";
import styles from './trendhim.module.scss';

export const Trendhim = () => {
    return (
        <div className={styles.container}>
            <div>
                As an intern, I contributed to modernizing the codebase by migrating styled components to scalable React components with SCSS modules.
            </div>
            <div>
                I also addressed user-reported issues, implemented new features, and defined global color theming to enhance consistency and maintainability.
            </div>
        </div>
    );
};